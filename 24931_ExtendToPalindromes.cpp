#include <iostream>

using namespace std;

char input[100005];
chat reversed[100005];

int main() {
    while(gets(input) && input[0]!=EOF) {
        int length = strlen(input);
        reverse_copy(input, length-input, )
    }





}
